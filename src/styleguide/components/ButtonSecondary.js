import React from 'react';
import { Button as Buttons } from '../../all-components-lib/components';

/**
 *
 * @visibleName Secondary Button
 */

export default function Button() {
  return (
    <Buttons variant="contained" color="secondary">
      Primary Button
    </Buttons>
  );
}
