import React from 'react';
import PageHeaderContent from '../components/PageHeaderContent';

const HomePage = () => {
  return (
    <PageHeaderContent
      title="Design System"
      description="A collection of reusable components, governed by clear standards, that can be assembled
  together to build any number of applications or products. It is the single source of truth
  which groups all the elements that will allow the teams to design, realize and develop a
  product."
    >
      Content
    </PageHeaderContent>
  );
};

export default HomePage;
